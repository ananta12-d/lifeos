# backend/requirements.txt

# Core
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Database
sqlalchemy>=2.0.0
alembic>=1.13.0

# Auth & Security
passlib[bcrypt]==1.7.4
python-jose[cryptography]>=3.3.0
python-multipart>=0.0.6

# Config & Validation
pydantic>=2.0.0
pydantic[email]>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# PostgreSQL driver (needed when deploying to Render)
# Comment this out if you don't have PostgreSQL installed locally
# psycopg2-binary>=2.9.0